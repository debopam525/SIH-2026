"""Report generation: inventory, algorithms, vulnerability/risk, exposure, recommendations,
migration roadmap, executive summary, and a combined 'full' report. JSON + CSV always; PDF
when the optional ``weasyprint`` dependency is installed (otherwise a standalone HTML file).
"""
