# Engineering notes

We know DES, 3DES and MD5 are weak and should be removed. RSA and ECDSA need a PQC plan.

This prose mentions many algorithm names but contains **no cryptographic API calls**, so a
correct scanner must not create any assets from this file.
